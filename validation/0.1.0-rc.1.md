# gruflo 0.1.0-rc.1 validation manifest

- Version: 0.1.0 (release candidate 1)
- Commit: e989f1346aac87ee5698b9af3023aead980e1583 (`feature/production-implementation`)
- Target: `x86_64-unknown-linux-gnu`
- Compiler: rustc 1.98.0 (stable); MSRV gate: rustc 1.96.0

## Deterministic gate

All commands ran fresh at the recorded commit and passed:

| Check | Result |
| --- | --- |
| `cargo fmt --check` | pass |
| `cargo clippy --all-targets --locked -- -D warnings` | pass, zero findings |
| `cargo test --all-targets --locked` | pass: 114 lib + 2 monitor journey + 8 output contract + 3 PTY = 127 tests, 0 failed |
| `cargo build --release --locked --target x86_64-unknown-linux-gnu` | pass |
| `cargo package --locked` | pass; crate contains `src/**`, `LICENSE`, `README.md`, `THIRD_PARTY_NOTICES.txt`, lockfile only |
| `python3 scripts/notices.py` | pass; 88 shipped crates, all within the accepted license policy |
| `cargo +1.96.0 build --all-targets --locked && cargo +1.96.0 test --lib` | pass |

## Non-interactive binary smoke (actual compiled binary)

Against the committed fixture host root (`GRUFLO_HOST_ROOT`, debug seam) and
the live host below:

| Path | Evidence |
| --- | --- |
| `--once` | one line per GPU in the contracted semantic order; exit 0 |
| `--tiny` | identity/activity/memory/hotspot/health line; exit 0 |
| `--json` | parses; schema_version 1; tagged observations; no `null`; sequence omitted; exit 0 |
| `--json-stream \| head -2` | two compact NDJSON records, sequence 1 and 2; broken pipe silent; exit 0 |
| SIGINT during `--json-stream` | exit 130, valid NDJSON prefix retained |
| unknown flag / malformed config / unknown `--gpu` selector | stderr diagnostic with path context; exit 2 |
| no discoverable GPU | one actionable stderr line; exit 1; no terminal takeover |

## Interactive PTY journeys (actual compiled binary)

`tests/pty_restoration.rs` drives the real binary on a real PTY:

1. normal quit (`q`): cursor restored before alternate-screen exit; exit 0;
2. SIGINT: restoration sequences present; exit 130;
3. injected post-acquisition fatal (`GRUFLO_FATAL_AFTER_MS`, debug seam):
   alternate-screen exit precedes the stderr diagnostic; exit 1.

Additional scripted PTY sessions exercised 120×40 mode (logo, exact tagline,
health sentence, selection marker), 70×20 compact, 30×3 tiny, arrow
selection, `t`/`m` cycling, and the `p`/`d`/`?` overlays; all quit cleanly.
Frame emulation of a 100×36 session reproduced the approved prototype
composition.

## Live hardware evidence

One live host was available during implementation and was exercised with the
release binary (not a substitute):

- Host: Linux 7.1.8-1-cachyos, in-tree `amdgpu`, AMD Radeon AI PRO R9700
  (Navi 48 / RDNA4, PCI `1002:7551`), discrete 32 GiB VRAM, `gpu_metrics`
  v1.3, no ROCm userspace requirement exercised; run as an ordinary user.
- Discovery and topology matched the host (one physical GPU, one partition).
- Cross-checks against kernel files: activity == `gpu_busy_percent`;
  memory == `mem_info_vram_used/_total`; hotspot == hwmon junction
  (`temp2_input`/1000); power cap == `power1_cap`/1e6; hotspot limit ==
  `temp2_crit`/1000.
- The SMU's intermittent idle throttle report (`throttle_status=0x2`,
  `indep_throttle_status` bit 36) surfaces as the factual sentence
  `thermal throttle active`; when the bits clear, health returns to
  `no active limits or faults`.
- Startup → first output over 20 release-binary samples: min 516 ms,
  median 518 ms, max 521 ms (budget: ≤ 1 s). A later run after the
  production-offset change measured 433 ms.
- 30 s `--json-stream` run: 122 records, zero sequence gaps, tick deltas
  p50/p95 = 250.0/250.1 ms, zero deltas ≥ 500 ms (no operation reached its
  cadence). Observation→assembly lag p95 = 125.0 ms after the production
  offset.
- TUI ran live and matched the approved visual composition; quit restored
  the terminal.

This run used live fixtures of opportunity, not a formal lab pass. The
workload-perturbation budget (≤ 2 % regression on a representative stable
GPU workload) was **not measured**: no repeatable representative workload
harness exists on this host. That measurement is the remaining external
prerequisite for claiming the discrete-RDNA regime fully qualified.

## Telemetry regime claims

| Regime | Claim |
| --- | --- |
| CDNA 1/2 (no XCP) | fixture-validated (`discrete-spx`, v1.3 blob, hwmon, RAS, throttle boundary) |
| CDNA 3/4 incl. XCP | fixture-validated (`multi-xcp`, v1.6 blob, socket/XCP scope, secondary-partition states) |
| discrete RDNA | fixture-validated plus the live evidence above; perturbation budget unmeasured, so not claimed fully qualified |
| Ryzen APU shared/GTT | fixture-validated (`apu-gtt` v2.4 centi-units + KFD FB_PUBLIC pool evidence, `apu-v3` v3.0 + carveout fallback) |

Process attribution: live HIP evidence from
`research/process-attribution/results/20260821T181207370038903Z/` remains the
authoritative process-overlay evidence; the overlay additionally passed the
sanitized fixtures reduced from it. No live process-overlay re-qualification
is claimed for this candidate.

## Skipped checks and limitations

- Workload perturbation: skipped (no representative workload on the host);
  named above as the remaining prerequisite for a qualified claim.
- AMD SMI enrichment against a real `libamd_smi.so`: not exercised live (no
  ROCm on the host); covered by the compiled fake-library ABI tests.
- aarch64: compile-check only in CI; no artifact, no support claim.
- First public release still requires one formally qualified `amdgpu` host
  per the validation contract.

## Artifacts

- Archive, `SHA256SUMS`, and provenance are produced by
  `.github/workflows/release.yml` from the immutable tag; no artifacts are
  claimed for this pre-tag candidate.
