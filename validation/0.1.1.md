# gpuflo 0.1.1 validation manifest

Release authorization: qualified

- Version: 0.1.1
- Commit: 1ef552f1f8cd74733c65ea8f46f5a4bd4e4fd728
- Target: `x86_64-unknown-linux-gnu`
- Compiler: rustc 1.98.0 (stable); MSRV gate: rustc 1.96.1
- Maximum required GLIBC: GLIBC_2.39

This manifest qualifies the 0.1.1 release from the versioned source commit above.
The tagged tree may add this manifest only; the release workflow verifies that
nothing outside `validation/` differs from the validated commit.

## Changes since 0.1.0

- Add fixture-backed Strix Halo (`1002:1586`) GTT accounting. Existing KFD
  classification remains unchanged for every other device.
- Add the opt-in `--cat` / `cat = true` sleeping-cat TUI decoration. It is
  presentation-only and avoids instrument surfaces, overlays, tiny mode, and
  terminals without enough free space.
- Add the repository GitHub Pages site and animated dashboard presentation.

## Deterministic gate

All commands ran fresh at the recorded commit and passed:

| Check | Result |
| --- | --- |
| `cargo fmt --check` | pass |
| `cargo clippy --all-targets --locked -- -D warnings` | pass, zero findings |
| `cargo test --all-targets --locked` | pass: 148 lib + 3 monitor journey + 8 output contract + 3 PTY = 162 tests, 0 failed |
| `cargo build --release --locked --target x86_64-unknown-linux-gnu` | pass |
| `cargo package --locked --allow-dirty` | pass; 34 files, 753.9 KiB uncompressed, 147.7 KiB compressed |
| `python3 scripts/notices.py` | pass; 88 shipped crates, 66 unique exact license/notice texts; committed file unchanged |
| `cargo +1.96 build --all-targets --locked` | pass |
| `cargo +1.96 test --lib --locked` | pass: 148 tests |

The fixture-backed integration tests use the `GPUFLO_HOST_ROOT` debug seam and
therefore run under the dev profile. The seam remains compiled out of release
binaries.

## Release-binary smoke

The actual `target/x86_64-unknown-linux-gnu/release/gpuflo` binary was exercised
on the live host below:

| Path | Evidence |
| --- | --- |
| `--version` | `gpuflo 0.1.1`; exit 0 |
| `--once` | one line for the detected R9700 with activity, VRAM, hotspot, power, clock, and source-backed throttle state; exit 0 |
| `--json` | parsed with `jq`; `schema_version` 1 and exactly one GPU; exit 0 |
| interactive `--cat` TUI | full mode rendered the live R9700 and sleeping cat; `q` restored the terminal and exited 0 |
| `objdump -T` | maximum required symbol version GLIBC_2.39 |

## Live hardware evidence

- Release qualification host: Linux 7.2.0-1-cachyos, in-tree `amdgpu`, AMD
  Radeon AI PRO R9700 (Navi 48 / RDNA4, PCI `1002:7551`), discrete 32 GiB
  VRAM; run as an ordinary user.
- Strix Halo source host: PCI `1002:1586`. Contributor verification reported
  `45.2/124.0 GiB GTT`; JSON reported pool `gtt` and total
  `133143986176`, exactly matching `mem_info_gtt_total` read from sysfs.

## Telemetry regime claims

| Regime | Claim |
| --- | --- |
| CDNA 1/2 (no XCP) | fixture-validated |
| CDNA 3/4 incl. XCP | fixture-validated |
| discrete RDNA | fixture-validated plus live R9700 evidence; perturbation budget unmeasured |
| Ryzen APU shared/GTT | fixture-validated |
| Strix Halo `1002:1586` | fixture-validated plus contributor live-host evidence |

## Skipped checks and limitations

- Workload perturbation (≤ 2% budget) remains unmeasured; no repeatable
  representative workload harness exists on the qualification host.
- AMD SMI enrichment against a real `libamd_smi.so` was not exercised live;
  compiled fake-library ABI tests cover the adapter.
- aarch64 receives a compile check only; no artifact or support claim.
- crates.io publication remains a separate manual credentialed step.

## Artifacts

The archive, `SHA256SUMS`, `GLIBC_BASELINE.txt`, and validation manifests are
produced by `.github/workflows/release.yml` from the immutable `v0.1.1` tag.
